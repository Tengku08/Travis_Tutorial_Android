language: android
android:
  components:
    - tools
    - build-tools-30.0.1
    - android-30
    - platform-tools

  licenses:
    - '.+'

sudo: required

jdk:
  - oraclejdk8

install: true

before_script:
  - chmod +x gradlew

script:
  - ./gradlew assembleRelease --stacktrace